import React from 'react';
import styles from './location.module.scss';

const OrderPageLocation = ({ className }) => {
  return <section className={styles.location}>OrderPageLocation</section>;
};

export default OrderPageLocation;
