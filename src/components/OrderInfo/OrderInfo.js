import React from 'react';
import './orderInfo.scss';

const OrderInfo = ({ className }) => {
  return <section className={`order-info ${className}`}>OrderInfo</section>;
};

export default OrderInfo;
